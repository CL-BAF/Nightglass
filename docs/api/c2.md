# API — C2

`honeywatch/c2/`.

## `c2/__init__.py`

```python
from honeywatch.c2 import Controller, C2Store, Worker, WorkerError, build_ssl_context, ensure_self_signed_pair
```

## `c2/store.py`

`honeywatch/c2/store.py:424`.

Schema: `c2_operations`, `c2_tasks`, `c2_workers`.

| Method | Detail |
|---|---|
| `create_operation(payload_id, target_ips, manifest, operation_id) -> Operation` | `op-<hex12>` |
| `get_operation(id) -> Operation|None` | |
| `update_operation_status(id, status, result)` | appends `result_log` |
| `list_operations(status, limit)` | |
| `create_task(task: WorkerTask)` | |
| `claim_next_task(worker_id, categories) -> WorkerTask|None` | atomic `UPDATE ... WHERE pending` |
| `complete_task(task_id, worker_id, success, result)` | `completed`/`failed` |
| `list_tasks(operation_id, status, worker_id, limit)` | |
| `register_worker(worker_id, categories)` | upsert |
| `heartbeat_worker(worker_id)` | |
| `list_workers(active_within_s)` | |
| `_task_from_row`, `_operation_from_row`, `_now`, `_new_id`, `_encode/_decode`, `_target_to_dict/_from_dict` | helpers |

## `c2/controller.py`

`honeywatch/c2/controller.py:411`.

```python
from honeywatch.c2.controller import Controller

ctl = Controller(store=C2Store("honeywatch.db"), host="0.0.0.0", port=8443, ssl_context=None, api_token=None)
await ctl.run()  # AppRunner + TCPSite, loops 3600 s
```

| Symbol | Detail |
|---|---|
| `_DASHBOARD_HTML: str` | CSS + JS WebSocket dashboard |
| `Controller.__init__` | validates `HAS_AIOHTTP`, `app=web.Application(middlewares=[auth])`, `_clients` |
| `_build_auth_middleware` | Bearer or `?token=` |
| Routes | `/`, `/ws`, `/api/health`, `/api/workers`, `/api/operations`, `/api/tasks`, `/api/tasks/claim`, `/api/tasks/{id}/result` |
| Handlers | `_dashboard`, `_api_health`, `_api_workers`, `_api_operations`, `_api_create_operation`, `_api_tasks`, `_api_claim_task`, `_api_task_result`, `_websocket`, `_push_snapshot`, `_broadcast` |

## `c2/tls.py`

`honeywatch/c2/tls.py:132`.

| Symbol | Detail |
|---|---|
| `NGINX_TEMPLATE: str` | upstream + ssl + ws upgrade |
| `generate_self_signed(cert, key, hostname, days=365)` | `openssl req -x509 -nodes -newkey rsa:2048` |
| `render_nginx_config(controller_port, cert_path, key_path, nginx_port, server_name) -> str` | |
| `write_nginx_config(path, ...)` | writes file |
| `ensure_self_signed_pair(base_dir, hostname) -> (cert,key)` | creates if missing |
| `build_ssl_context(cert, key) -> SSLContext|None` | `create_default_context` + `load_cert_chain` |

## `c2/worker.py`

`honeywatch/c2/worker.py:332`.

```python
from honeywatch.c2.worker import Worker, WorkerError

worker = Worker(controller_url="http://127.0.0.1:8443", worker_id=None, categories=None, exec_mode="dry_run", poll_interval=5.0, ssh_key=None, ssh_user="root", api_token=None)
await worker.run()  # polling or websocket
worker.claim_task() -> WorkerTask|None
worker.report_result(task_id, success, result)
worker.execute_task(task) -> dict
worker.stop()
```

| Method | Detail |
|---|---|
| `_request(method, path, data, timeout)` | `urllib` with Bearer, 204 handling, `URLError→WorkerError` |
| `_run_shell(script, target)` | temp `.sh` `chmod 700` `/bin/sh` 600 s |
| `_run_ssh(script, target)` | `ssh -o StrictHostKeyChecking=no -o BatchMode=yes -i key user@ip` |
| `_run_polling()` | loop claim→execute→report |
| `_run_websocket()` | ws transport, `register_worker`, fallback HTTP claim |
| `_task_from_dict(data) -> WorkerTask` | helper |

See [C2 Controller](../c2.md).
